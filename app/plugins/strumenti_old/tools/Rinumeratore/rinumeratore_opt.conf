numero_def = ca_objects.num_def_numero

isad4_exce = [279, 368, 369, 370, 1879, 1880, 1881, 1882]

isad4 = UNITA SEMPLICI

categorie = {
    Fondi = {
        def = {
            container = ca_objects.num_def_numero,
            numero = ca_objects.num_def_numero,
            # prefisso = ca_objects.numerazione_definitiva.prefix
        }
    },
    LIVELLI = {
        def = {
            container = ca_objects.num_def_numero,
            numero = ca_objects.num_def_numero,
            # prefisso = ca_objects.numerazione_definitiva.prefix,
            # romano = ca_objects.numerazione_definitiva.num_romano
        },
        option=[romano]
    },
    UNITA COMPLESSE = {
        def = {
            container = ca_objects.num_def_numero,
            numero = ca_objects.num_def_numero,
            # prefisso = ca_objects.numerazione_definitiva.prefix,
        }
    },
    UNITA SEMPLICI = {
        def = {
            container = ca_objects.num_def_numero,
            numero = ca_objects.num_def_numero,
            # prefisso = ca_objects.numerazione_definitiva.prefix,
        },
        unita = true
    }
}
